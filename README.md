# JavaDataLens
console application that can read large text files, perform various complex analyses (word count, specific pattern matching, etc.) on the data concurrently, and then save the detailed reports to a new file.
